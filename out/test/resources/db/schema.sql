DROP TABLE IF EXISTS SECURITY_DEFINITIONS;

CREATE TABLE SECURITY_DEFINITIONS (
--   ID INT AUTO_INCREMENT  PRIMARY KEY,
  TICKER CHAR(255) UNIQUE NOT NULL,
  SECURITY_TYPE CHAR(255) NOT NULL,
  STD_DEV DECIMAL(19,2),
  EXP_RETURN DECIMAL(19,2),
  MATURITY DECIMAL(19,2),
  STRIKE DECIMAL(19,2)
);
